
let button = document.getElementById('button')

button.addEventListener('click', () => {
    // variable in lexical scope get refresh value when click event generated.
    let input_value = document.getElementById('input').value
    // and you can use this value for cheking invaliable or form data.
})