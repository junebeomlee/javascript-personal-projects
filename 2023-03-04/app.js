import Serach from "./search.js"

// 함수형
export default function App($taget) {
    this.state = {
        count: 0
    }
    this.setState = (nextState) => {

    }
    console.log($taget)
    // const search = new Serach({$t, initialState: "react"})
}